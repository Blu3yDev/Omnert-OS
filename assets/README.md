# Assets

Shared source artwork and branding assets live here. `brand/omnert-logo.png` is
the canonical supplied Omnert mark; the image build installs it into
`/usr/share/omnertos/brand/` and exposes it through the system icon theme.
