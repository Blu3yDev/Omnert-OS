# Themes

Shared Omnert design contracts live here. See [tokens.md](tokens.md) for the
prototype palette, geometry, typography, and motion rules. Toolkit-specific
styles currently ship through the live filesystem overlay in `distro/`.
