# Tests

Validation will include static image-definition checks and opt-in VM boot tests.
No test performs disk installation or formatting.

Run the host-independent checks with:

```bash
python3 tests/validate.py
```
